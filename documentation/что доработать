Что нужно доработать:

Фронт:

В компоненте настроек добавить новые поля: «Перерывы между занятиями (мин)» и «Использовать перерывы (чекбокс)».

Сделать UI для «утреннего опроса»: select для часов/минут (например 07:30), вместо голого поля cron.

API / БД:

В таблице settings добавить поля break_enabled (bool) и break_minutes (int).

На стороне API обновить схему Settings, чтобы эти поля возвращались и сохранялись.

Для cron: добавить вспомогательную логику, которая хранит строку cron, но принимает/отдаёт удобочитаемый вид (например {"hour":7,"minute":30}).